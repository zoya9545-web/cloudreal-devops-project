apiVersion: apps/v1
kind: Deployment

metadata:
  name: cloudreal-app

spec:
  replicas: 2

  selector:
    matchLabels:
      app: cloudreal-app

  template:
    metadata:
      labels:
        app: cloudreal-app

    spec:
      containers:
      - name: cloudreal-app
        image: zoya9545/cloudreal-app:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3002
